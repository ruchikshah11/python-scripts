# Python Script Index

Auto-generated index of every script under this repo, grouped by category and subfolder.
Regenerated by [Projects/IndexGenerator/generate_index.py](Projects/IndexGenerator/generate_index.py) - do not hand-edit this file; re-run that script instead.

**108 scripts across 8 categories**

## Contents

- [SharePoint (28)](#sharepoint)
- [Patterns (26)](#patterns)
- [Projects (22)](#projects)
- [Database (13)](#database)
- [Learning (9)](#learning)
- [DesktopApps (8)](#desktopapps)
- [HelloWorld (1)](#helloworld)
- [Utilities (1)](#utilities)

---

## SharePoint

### AddListItem

- [`add_list_item.py`](SharePoint/AddListItem/add_list_item.py) — Adds a new item to a SharePoint list. Uses the sharepoint library (SharePoint/sharepoint) for the SharePoint calls, and the shared sp_logging helper (SharePoint/common) for logging.

### common

- [`sp_logging.py`](SharePoint/common/sp_logging.py) — Shared logging helper. Import this from any script under SharePoint/ instead of duplicating the daily-log-file / retention / CorrelationID boilerplate.

### ConnectToSite

- [`connect_to_site.py`](SharePoint/ConnectToSite/connect_to_site.py) — Connects to a SharePoint site interactively (browser sign-in) and prints the site's Title and Url. Uses the sharepoint library (SharePoint/sharepoint) for the connection, and the shared sp_logging helper (SharePoint/common) for logging.

### DeleteListItem

- [`delete_list_item.py`](SharePoint/DeleteListItem/delete_list_item.py) — Deletes a SharePoint list item by ID - to the Recycle Bin by default, or permanently with --permanent. Destructive - prompts for confirmation unless --yes is passed. Uses the sharepoint library (SharePoint/sharepoint) for the SharePoint calls, and the shared sp_logging helper (SharePoint/common) ...

### Demo

- [`demo_export_term_group.py`](SharePoint/Demo/demo_export_term_group.py) — Demo script for sharepoint.export_term_store() and export_term_group() - direct usage example with plain print() output. No CLI args, no logging scaffolding.
- [`demo_file_transfer.py`](SharePoint/Demo/demo_file_transfer.py) — Demo for file upload/download: upload_file, download_file. Self-contained - creates a small temp local text file, uploads it, downloads it back to a different local path, and verifies the content round-trips correctly. No CLI args, no logging scaffolding.
- [`demo_item_permissions.py`](SharePoint/Demo/demo_item_permissions.py) — Demo for list item permission operations: break_item_role_inheritance, get_item_role_assignments, add_item_role, remove_item_role, reset_item_role_inheritance. Runs a full cycle and resets the item back to inherited permissions at the end, so it's safe to re-run. No CLI args, no logging scaffolding.
- [`demo_list_item_crud.py`](SharePoint/Demo/demo_list_item_crud.py) — Demo for list item write operations: add_list_item, update_list_item (normal + system_update), delete_list_item. Self-contained and safe to re-run - it creates its own throwaway item, updates it twice, then deletes it (to the Recycle Bin, not permanently) at the end. No CLI args, no logging scaff...
- [`demo_sharepoint.py`](SharePoint/Demo/demo_sharepoint.py) — Demo script for the sharepoint library - exercises the read-only functions directly (connect, get_connection_info, get_lists, get_list, get_list_items, get_fields, get_content_types, get_groups, get_associated_group, get_group_members) with plain print() output. No CLI args, no logging scaffoldin...

### DownloadFile

- [`download_file.py`](SharePoint/DownloadFile/download_file.py) — Downloads a file from a SharePoint document library to a local path. Uses the sharepoint library (SharePoint/sharepoint) for the SharePoint calls, and the shared sp_logging helper (SharePoint/common) for logging.

### ExportTermGroup

- [`export_term_group.py`](SharePoint/ExportTermGroup/export_term_group.py) — Exports taxonomy term group(s) (term sets + terms) to a JSON file for backup/reference - one specific group via --group-name, or every group in the term store if omitted. NOT equivalent to PnP PowerShell's Export-PnPTermGroupToXml - that produces PnP's own provisioning-template XML schema, which ...

### GetConnection

- [`get_connection.py`](SharePoint/GetConnection/get_connection.py) — Connects to a SharePoint site interactively and prints details about the active connection - site URL, connected user, tenant, client ID, token type, and token expiry. Equivalent in spirit to PowerShell's Get-PnPConnection. Uses the sharepoint library (SharePoint/sharepoint) for the connection, a...

### GetContentTypes

- [`get_content_types.py`](SharePoint/GetContentTypes/get_content_types.py) — Gets site content types. Equivalent to PnP PowerShell's Get-PnPContentType. Uses the sharepoint library (SharePoint/sharepoint) for the SharePoint calls, and the shared sp_logging helper (SharePoint/common) for logging.

### GetField

- [`get_field.py`](SharePoint/GetField/get_field.py) — Gets site or list fields (columns). Equivalent to PnP PowerShell's Get-PnPField. Uses the sharepoint library (SharePoint/sharepoint) for the SharePoint calls, and the shared sp_logging helper (SharePoint/common) for logging.

### GetGroup

- [`get_group.py`](SharePoint/GetGroup/get_group.py) — Gets SharePoint groups, an associated Owner/Member/Visitor group, or a group's members. Equivalent to PnP PowerShell's Get-PnPGroup / Get-PnPGroupMembers. Uses the sharepoint library (SharePoint/sharepoint) for the SharePoint calls, and the shared sp_logging helper (SharePoint/common) for logging.

### GetList

- [`get_list.py`](SharePoint/GetList/get_list.py) — Connects to a SharePoint site and retrieves list metadata - either every list on the site, or a single list's details when --list-title is given. Uses the sharepoint library (SharePoint/sharepoint) for the SharePoint calls, and the shared sp_logging helper (SharePoint/common) for logging.

### GetListItems

- [`get_list_items.py`](SharePoint/GetListItems/get_list_items.py) — Connects to a SharePoint site and retrieves items from a specific list. Uses the sharepoint library (SharePoint/sharepoint) for the SharePoint calls, and the shared sp_logging helper (SharePoint/common) for logging.

### SetItemPermission

- [`set_item_permission.py`](SharePoint/SetItemPermission/set_item_permission.py) — Manages a list item's permissions: break/reset inheritance, view current role assignments, or add/remove a role for a principal. Equivalent to PnP PowerShell's Get-PnPListItemPermission / Set-PnPListItemPermission. Destructive for break/reset/add-role/remove-role - prompts for confirmation unless...

### sharepoint

- [`__init__.py`](SharePoint/sharepoint/__init__.py) — sharepoint - personal SharePoint helper library (Ruchik Shah).
- [`connection.py`](SharePoint/sharepoint/connection.py) — Connection helpers - interactive sign-in and ClientContext creation.
- [`content_types.py`](SharePoint/sharepoint/content_types.py) — Content type read helpers.
- [`fields.py`](SharePoint/sharepoint/fields.py) — Field (site column) read helpers.
- [`files.py`](SharePoint/sharepoint/files.py) — File upload/download helpers.
- [`lists.py`](SharePoint/sharepoint/lists.py) — List and list-item read/write helpers.
- [`permissions.py`](SharePoint/sharepoint/permissions.py) — Permissions helpers - groups, group membership, and list-item role assignments.
- [`taxonomy.py`](SharePoint/sharepoint/taxonomy.py) — Taxonomy export helpers.

### UpdateListItem

- [`update_list_item.py`](SharePoint/UpdateListItem/update_list_item.py) — Updates an existing SharePoint list item by ID. Uses the sharepoint library (SharePoint/sharepoint) for the SharePoint calls, and the shared sp_logging helper (SharePoint/common) for logging.

### UploadFile

- [`upload_file.py`](SharePoint/UploadFile/upload_file.py) — Uploads a local file (up to 4MB) into a SharePoint document library folder. Uses the sharepoint library (SharePoint/sharepoint) for the SharePoint calls, and the shared sp_logging helper (SharePoint/common) for logging.

## Patterns

### AlphabetTriangle

- [`alphabet_triangle.py`](Patterns/AlphabetTriangle/alphabet_triangle.py) — Prints an alphabet triangle - row i contains letters A through the i-th letter concatenated (A / AB / ABC / ...), sized by --rows.

### ButterflyPattern

- [`butterfly_pattern.py`](Patterns/ButterflyPattern/butterfly_pattern.py) — Prints a butterfly pattern (two triangles mirrored side by side, widening then narrowing), sized by --rows (half-height).

### CharacterPattern

- [`character_pattern.py`](Patterns/CharacterPattern/character_pattern.py) — Prints an ASCII-art pattern shaped like any single character you give it - letter, digit, or symbol. --char R prints an R-shaped pattern, --char O prints an O-shaped pattern, --char 1 prints a 1-shaped pattern, and so on for any character your system's font supports. Works by rendering the charac...

### Checkerboard

- [`checkerboard.py`](Patterns/Checkerboard/checkerboard.py) — Prints a checkerboard pattern of stars and spaces, sized by --rows (a --rows x --rows grid).

### CrossPattern

- [`cross_pattern.py`](Patterns/CrossPattern/cross_pattern.py) — Prints an X (cross) sign made of stars in a (2*rows-1) x (2*rows-1) grid, sized by --rows (half-width from the center).

### Diamond

- [`diamond.py`](Patterns/Diamond/diamond.py) — Prints a solid, centered diamond of stars (a pyramid on top of an inverted pyramid), sized by --rows (half-height).

### FloydsTriangle

- [`floyds_triangle.py`](Patterns/FloydsTriangle/floyds_triangle.py) — Prints Floyd's Triangle - sequential numbers filling a triangle, row i has i numbers, counting continues across rows, sized by --rows.

### HeartPattern

- [`heart_pattern.py`](Patterns/HeartPattern/heart_pattern.py) — Prints a heart shape made of stars, using the implicit heart curve (x^2 + y^2 - 1)^3 - x^2*y^3 <= 0 rasterized onto a text grid, rather than hand-tuned spacing/star counts (which is fiddly to get looking like an actual heart). Sized by --size (roughly half-height).

### HollowDiamond

- [`hollow_diamond.py`](Patterns/HollowDiamond/hollow_diamond.py) — Prints a hollow diamond outline of stars (edges only, blank interior), sized by --rows (half-height).

### HollowPyramid

- [`hollow_pyramid.py`](Patterns/HollowPyramid/hollow_pyramid.py) — Prints a hollow pyramid outline of stars (edges only, solid base), sized by --rows.

### HollowSquare

- [`hollow_square.py`](Patterns/HollowSquare/hollow_square.py) — Prints a hollow square outline of stars (border only, blank interior), sized by --rows.

### HollowTriangle

- [`hollow_triangle.py`](Patterns/HollowTriangle/hollow_triangle.py) — Prints a hollow left-aligned right triangle outline of stars (edges only, solid base), sized by --rows.

### Hourglass

- [`hourglass.py`](Patterns/Hourglass/hourglass.py) — Prints an hourglass pattern of stars (a solid inverted pyramid on top of a solid pyramid - the opposite of Diamond), sized by --rows (half-height).

### InvertedPyramid

- [`inverted_pyramid.py`](Patterns/InvertedPyramid/inverted_pyramid.py) — Prints a solid, centered inverted pyramid of stars, sized by --rows.

### InvertedTriangle

- [`inverted_triangle.py`](Patterns/InvertedTriangle/inverted_triangle.py) — Prints a left-aligned inverted right triangle of stars (--rows stars shrinking to 1 star), sized by --rows.

### LeftTriangle

- [`left_triangle.py`](Patterns/LeftTriangle/left_triangle.py) — Prints a left-aligned right triangle of stars (1 star growing to --rows stars), sized by --rows.

### NumberPyramid

- [`number_pyramid.py`](Patterns/NumberPyramid/number_pyramid.py) — Prints a number pyramid - row i repeats the digit i, i times (1 / 22 / 333 / ...), sized by --rows.

### NumberTriangle

- [`number_triangle.py`](Patterns/NumberTriangle/number_triangle.py) — Prints a number triangle - row i contains the digits 1 through i concatenated (1 / 12 / 123 / ...), sized by --rows.

### PalindromicPattern

- [`palindromic_pattern.py`](Patterns/PalindromicPattern/palindromic_pattern.py) — Prints a centered palindromic number pyramid (1 / 121 / 12321 / ...), sized by --rows.

### PascalsTriangle

- [`pascals_triangle.py`](Patterns/PascalsTriangle/pascals_triangle.py) — Prints Pascal's Triangle (each number is the sum of the two above it), centered, sized by --rows.

### PlusPattern

- [`plus_pattern.py`](Patterns/PlusPattern/plus_pattern.py) — Prints a plus/cross (+) sign made of stars in a (2*rows-1) x (2*rows-1) grid, sized by --rows (half-width from the center).

### Pyramid

- [`pyramid.py`](Patterns/Pyramid/pyramid.py) — Prints a solid, centered pyramid of stars, sized by --rows.

### Rhombus

- [`rhombus.py`](Patterns/Rhombus/rhombus.py) — Prints a rhombus (slanted square/parallelogram) of stars, sized by --rows.

### RightAlignedTriangle

- [`right_aligned_triangle.py`](Patterns/RightAlignedTriangle/right_aligned_triangle.py) — Prints a right-aligned right triangle of stars (1 star growing to --rows stars, aligned to the right edge), sized by --rows.

### SpiralMatrix

- [`spiral_matrix.py`](Patterns/SpiralMatrix/spiral_matrix.py) — Prints a --rows x --rows grid filled with 1, 2, 3, ... in spiral order (starting top-left, spiraling clockwise inward).

### Square

- [`square.py`](Patterns/Square/square.py) — Prints a solid square pattern of stars, sized by --rows.

## Projects

### CurrencyConverter

- [`convert_currency.py`](Projects/CurrencyConverter/convert_currency.py) — Converts an amount between currencies using the free Frankfurter API (European Central Bank reference rates) - no API key required. Can also list every supported currency code. Provides logging (with daily log rotation, retention, and per-run CorrelationID), module dependency checking, and error ...

### DuplicateFinder

- [`find_duplicates.py`](Projects/DuplicateFinder/find_duplicates.py) — Finds exact duplicate files under a folder (recursively) by content hash, and reports them grouped together with wasted space. Can also delete duplicates (keeping one copy per group) - defaults to a dry run; pass --delete to actually remove files, with a confirmation prompt unless --yes is also p...

### EmailNotifier

- [`send_notification.py`](Projects/EmailNotifier/send_notification.py) — Sends an email notification via SMTP. Credentials are read from environment variables - never hardcoded, never passed as CLI arguments (which would leak into shell history).

### EmailTemplateGenerator

- [`generate_email_templates.py`](Projects/EmailTemplateGenerator/generate_email_templates.py) — Generates the bs one QM bilingual (German + English) email notification templates - one HTML file per notification type, matching the approved design (right-aligned "bs one: quality" header, white card with a German block, a divider, then an English block, and a bilingual footer).

### EmailTemplateManifestGenerator

- [`generate_combined_manifest.py`](Projects/EmailTemplateManifestGenerator/generate_combined_manifest.py) — Generates the PnP provisioning XML manifest for the bs one QM combined (bilingual, DE / EN) email templates - one <pnp:File> entry per template, pointing at Files/Combined/<TemplateName>.html.

### ExpenseTracker

- [`expense_tracker.py`](Projects/ExpenseTracker/expense_tracker.py) — A CLI expense tracker backed by SQLite - add, list, summarize (by category, with an optional month filter), and delete expenses. The database file (expenses.db) is created automatically next to this script on first use.

### FileOrganizer

- [`organize_files.py`](Projects/FileOrganizer/organize_files.py) — Organizes files in a folder into subfolders, either by type (Images, Documents, Videos, Audio, Archives, Scripts, Other) or by last-modified date (YYYY-MM subfolders). Defaults to a dry run that only PRINTS what would happen - pass --apply to actually move files. Only processes files directly ins...

### GitHubStats

- [`github_stats.py`](Projects/GitHubStats/github_stats.py) — Pulls public GitHub stats for a user (aggregate across all public repos: total stars/forks, language breakdown, top repos) or for one specific repo (stars, forks, open issues, language, description). No API key required - uses GitHub's public REST API, subject to its unauthenticated rate limit (6...

### LogAnalyzer

- [`analyze_logs.py`](Projects/LogAnalyzer/analyze_logs.py) — Scans every Logs/ folder under a project root (recursively) and summarizes runs, successes, failures, durations, and error counts per script - across all the scripts in this workspace that use the shared daily-log-file/CorrelationID logging pattern. Provides logging (with daily log rotation, rete...

### Matrix

- [`matrix_addition.py`](Projects/Matrix/MatrixAddition/matrix_addition.py) — Adds two matrices (Matrix A is RowsA x ColsA, Matrix B is RowsB x ColsB - both dimensions must match exactly for addition) and prints Matrix A, Matrix B, and the sum.
- [`matrix_multiplication.py`](Projects/Matrix/MatrixMultiplication/matrix_multiplication.py) — Multiplies two matrices (Matrix A is RowsA x ColsA, Matrix B is RowsB x ColsB - not necessarily square, and not necessarily the same size as each other, as long as ColsA == RowsB) using the classic triple-loop algorithm, and prints Matrix A, Matrix B, and the product.

### PasswordTool

- [`password_tool.py`](Projects/PasswordTool/password_tool.py) — Generates cryptographically secure random passwords (using the `secrets` module, not `random`), or checks the strength of an existing password and gives specific feedback.

### PSTryCatchAudit

- [`ps_trycatch_audit.py`](Projects/PSTryCatchAudit/ps_trycatch_audit.py) — Finds PowerShell functions whose entire body got wrapped in a new try/catch block, by comparing a working-tree file against its git HEAD version.

### QRCodeGenerator

- [`generate_qr.py`](Projects/QRCodeGenerator/generate_qr.py) — Generates a QR code image (PNG) encoding any text or URL.

### RandomQuote

- [`random_quote.py`](Projects/RandomQuote/random_quote.py) — Gets a random quote, general joke, or Chuck Norris joke - no API key required. Uses ZenQuotes (quotes), Official Joke API (jokes), and api.chucknorris.io (Chuck Norris jokes), all free and keyless.

### RegexTester

- [`regex_tester.py`](Projects/RegexTester/regex_tester.py) — Tests a regex pattern against sample text and shows every match, its position, and any numbered/named capture groups.

### ReportBuilder

- [`build_report.py`](Projects/ReportBuilder/build_report.py) — Builds a formatted PDF or Excel report from a CSV file - the output format is inferred from the --output file extension (.pdf or .xlsx).

### TodoManager

- [`todo_manager.py`](Projects/TodoManager/todo_manager.py) — A CLI to-do list manager backed by SQLite - add, list, complete, and delete tasks. The database file (todo.db) is created automatically next to this script on first use.

### UnitConverter

- [`convert_units.py`](Projects/UnitConverter/convert_units.py) — Converts a value between units of length, weight, or temperature.

### UptimeMonitor

- [`monitor_uptime.py`](Projects/UptimeMonitor/monitor_uptime.py) — Checks one or more URLs and reports whether each is up (status < 400) or down (status >= 400, or a network error), with response time. Can run a single pass (default) or repeated rounds (--count N --interval seconds) within one execution, logging state changes (up -> down / down -> up) as they ha...

### Weather

- [`get_weather.py`](Projects/Weather/get_weather.py) — Gets current weather plus a multi-day forecast for any location by name - no API key required. Looks up coordinates via Open-Meteo's free geocoding API, then fetches current conditions and a daily forecast for those coordinates from Open-Meteo's free forecast API. Provides logging (with daily log...
- [`weather_app.py`](Projects/Weather/weather_app.py) — A Tkinter desktop weather app with a clean, card-style UI - built for end users rather than the terminal. Reuses get_weather.py's tested functions directly (geocode_location, get_weather_data, WEATHER_CODE_DESCRIPTIONS) - no weather logic is duplicated here, this file only adds the visual layer.

## Database

### SqlScripts

- [`backup_sql_database.py`](Database/SqlScripts/BackupSqlDatabase/backup_sql_database.py) — Backs up the tenant database via BACKUP DATABASE. Uses the sql_common library (Database/SqlScripts/common) for the SQL Server calls, and the shared db_logging helper (Database/SqlScripts/common) for logging.
- [`db_logging.py`](Database/SqlScripts/common/db_logging.py) — Shared logging helper. Import this from any script under Database/SqlScripts/ instead of duplicating the daily-log-file / retention / CorrelationID boilerplate.
- [`export_sql_query_to_csv.py`](Database/SqlScripts/ExportSqlQueryToCsv/export_sql_query_to_csv.py) — Runs a SQL query against the tenant database and exports the results to CSV. Uses the sql_common library (Database/SqlScripts/common) for the SQL Server calls, and the shared db_logging helper (Database/SqlScripts/common) for logging.
- [`export_tenant_subscriptions_to_csv.py`](Database/SqlScripts/ExportTenantSubscriptionsToCsv/export_tenant_subscriptions_to_csv.py) — Exports Common.Tenant.Subscription rows from every tenant database into one CSV. Uses the sql_common library (Database/SqlScripts/common) for the SQL Server calls, and the shared db_logging helper (Database/SqlScripts/common) for logging.
- [`get_tenant_database_mapping.py`](Database/SqlScripts/GetTenantDatabaseMapping/get_tenant_database_mapping.py) — Lists each tenant with the database it's configured to use. Uses the sql_common library (Database/SqlScripts/common) for the SQL Server calls, and the shared db_logging helper (Database/SqlScripts/common) for logging.
- [`import_csv_to_sql.py`](Database/SqlScripts/ImportCsvToSql/import_csv_to_sql.py) — Bulk-imports a CSV file into a table in the tenant database. Uses the sql_common library (Database/SqlScripts/common) for the SQL Server calls, and the shared db_logging helper (Database/SqlScripts/common) for logging.
- [`invoke_sql_delete.py`](Database/SqlScripts/InvokeSqlDelete/invoke_sql_delete.py) — Runs a DELETE statement against the tenant database. Uses the sql_common library (Database/SqlScripts/common) for the SQL Server calls, and the shared db_logging helper (Database/SqlScripts/common) for logging.
- [`invoke_sql_insert.py`](Database/SqlScripts/InvokeSqlInsert/invoke_sql_insert.py) — Runs an INSERT statement against the tenant database. Uses the sql_common library (Database/SqlScripts/common) for the SQL Server calls, and the shared db_logging helper (Database/SqlScripts/common) for logging.
- [`invoke_sql_script_file.py`](Database/SqlScripts/InvokeSqlScriptFile/invoke_sql_script_file.py) — Runs a multi-batch .sql file (e.g. a migration script) against the tenant database. Uses the sql_common library (Database/SqlScripts/common) for the SQL Server calls, and the shared db_logging helper (Database/SqlScripts/common) for logging.
- [`invoke_sql_select.py`](Database/SqlScripts/InvokeSqlSelect/invoke_sql_select.py) — Runs a SELECT statement against the tenant database and prints the results. Uses the sql_common library (Database/SqlScripts/common) for the SQL Server calls, and the shared db_logging helper (Database/SqlScripts/common) for logging.
- [`invoke_sql_update.py`](Database/SqlScripts/InvokeSqlUpdate/invoke_sql_update.py) — Runs an UPDATE statement against the tenant database. Uses the sql_common library (Database/SqlScripts/common) for the SQL Server calls, and the shared db_logging helper (Database/SqlScripts/common) for logging.
- [`sql_common.py`](Database/SqlScripts/common/sql_common.py) — Shared SQL Server helpers used by every script under Database/SqlScripts/. Wraps the connect/open/close boilerplate (via pyodbc) so fixes and improvements only need to happen in one place. Python port of SqlScripts.Common.psm1.
- [`update_effort_entry_type.py`](Database/SqlScripts/UpdateEffortEntryType/update_effort_entry_type.py) — One-off fix: sets EntryTypeID = 1 on specific Project.Activity.Effort rows. Uses the sql_common library (Database/SqlScripts/common) for the SQL Server calls, and the shared db_logging helper (Database/SqlScripts/common) for logging.

## Learning

### Lesson01_VariablesAndTypes

- [`lesson01.py`](Learning/Lesson01_VariablesAndTypes/lesson01.py) — Lesson 1: Variables, Types, and Basic I/O - variables, types, f-strings, print()/input(), and numeric operators.

### Lesson02_ControlFlow

- [`lesson02.py`](Learning/Lesson02_ControlFlow/lesson02.py) — Lesson 2: Control Flow - if/elif/else, comparison and logic operators, for/while loops, enumerate(), break/continue. Python has no braces {} for blocks - indentation IS the block, and a line starting one ends with ":"

### Lesson03_Functions

- [`lesson03.py`](Learning/Lesson03_Functions/lesson03.py) — Lesson 3: Functions - def, explicit return, default/keyword arguments, docstrings, *args/**kwargs, and lambda.

### Lesson04_DataStructures

- [`lesson04.py`](Learning/Lesson04_DataStructures/lesson04.py) — Lesson 4: Data Structures - list (indexing, slicing, comprehensions), tuple, dict, set, and nested structures.

### Lesson05_ErrorHandling

- [`lesson05.py`](Learning/Lesson05_ErrorHandling/lesson05.py) — Lesson 5: Error Handling - try/except/else/finally, raising exceptions with raise, and custom exception classes.

### Lesson06_FilesAndModules

- [`lesson06.py`](Learning/Lesson06_FilesAndModules/lesson06.py) — Lesson 6: Files, JSON/CSV, and Modules - text file read/write, json.dump/ load/dumps, csv.DictWriter/DictReader, and standard-library vs pip- installed modules.

### Lesson07_RestApiPractical

- [`lesson07.py`](Learning/Lesson07_RestApiPractical/lesson07.py) — Lesson 7: Practical - Calling a REST API with `requests` - GET/POST, query params, JSON bodies, custom headers, and raise_for_status().

### Lesson08_StringOperations

- [`string_operations.py`](Learning/Lesson08_StringOperations/string_operations.py) — Reference and demo of Python's built-in string operations - creation, indexing/slicing, case conversion, searching, trimming, splitting/joining, replacing, formatting, padding, type checks, encoding, and translation.

### Lesson09_Operators

- [`operators.py`](Learning/Lesson09_Operators/operators.py) — Reference and demo of Python's operators - arithmetic, comparison, logical, bitwise, assignment (including augmented and walrus), identity, and membership operators, plus a note on precedence.

## DesktopApps

### PatternVisualizer

- [`pattern_visualizer.py`](DesktopApps/PatternVisualizer/pattern_visualizer.py) — An interactive Tkinter viewer for every pattern in the Patterns folder - pick a pattern from the dropdown, drag the --rows/--size slider, and see it update live. Reuses each pattern's actual print_xxx() function directly (via stdout capture), rather than reimplementing any pattern's logic here.

### PersonalDashboard

- [`personal_dashboard.py`](DesktopApps/PersonalDashboard/personal_dashboard.py) — A single-window Tkinter "at a glance" dashboard combining current weather, pending to-do tasks, and this month's expense summary - all refreshing automatically on open. Reuses the tested functions/data files from Weather, TodoManager, and ExpenseTracker directly.

### SharePointExplorer

- [`sharepoint_explorer.py`](DesktopApps/SharePointExplorer/sharepoint_explorer.py) — A Tkinter desktop client over the `sharepoint` library (SharePoint/sharepoint) - connect to a site, browse its lists, view items in a selected list, edit an item's Title, and upload a file to a document library. Reuses the same tested library functions as the standalone SharePoint CLI scripts - n...

### SPDummyDataGenerator

- [`item_creator.py`](DesktopApps/SPDummyDataGenerator/item_creator.py) — Creates dummy list items in batches - queues `batch_size` items' worth of add_item() calls, then flushes with a single execute_query(), same batching model as the C# tool's ListItemCreator (its "Batch size" setting controls how many items accumulate before a network round-trip).
- [`schema_reader.py`](DesktopApps/SPDummyDataGenerator/schema_reader.py) — Reads a SharePoint list's writable, populatable fields - the schema that drives value_generator.py / item_creator.py. Hidden fields, read-only fields, and an explicit skip-list of system internal names are excluded, mirroring the same field-selection rules as the original C# SPDummyDataGenerator ...
- [`spdummydatagenerator.py`](DesktopApps/SPDummyDataGenerator/spdummydatagenerator.py) — A Tkinter desktop tool that connects to a SharePoint list and populates it with realistic random ("dummy") data - covering every field type the REST API can write (Text, Note, Choice, MultiChoice, Number, Currency, Boolean, DateTime, URL, Guid, Geolocation, Person/Group, Lookup, and Managed Metad...
- [`value_generator.py`](DesktopApps/SPDummyDataGenerator/value_generator.py) — Generates a random value for each supported field kind schema_reader.py returns, using the Faker library - Python's rough equivalent of the C# tool's Bogus library. Reference data needed to generate realistic values (site users, lookup-list candidate items, Managed Metadata terms) is fetched once...

### UtilityToolboxGUI

- [`toolbox_gui.py`](DesktopApps/UtilityToolboxGUI/toolbox_gui.py) — A Tkinter desktop app that wraps several of the standalone CLI utilities built in this workspace (Weather, Currency Converter, Unit Converter, QR Code Generator, Password Tool, To-Do Manager, Expense Tracker) behind one tabbed window - reusing their tested functions directly rather than reimpleme...

## HelloWorld

- [`hello_world.py`](HelloWorld/hello_world.py)

## Utilities

### Templates

- [`ScriptTemplate.py`](Utilities/Templates/ScriptTemplate/ScriptTemplate.py) — Basic Structure / Starter Template - Starting point for any new Python script. Provides logging (with daily log rotation, retention, and per-run CorrelationID), module dependency checking, and error handling.
